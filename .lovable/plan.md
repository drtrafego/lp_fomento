

# Ajustar barra de lote: 96% com fundo visível dos 4% restantes

## Alteração em `src/components/BelowFoldSections.tsx` (linhas 783-795)

- Trocar `98%` por `96%` no texto e na largura da barra
- Trocar o fundo da barra de `bg-secondary` para um fundo mais claro/visível (ex: `bg-white/15`) para que os 4% restantes fiquem evidentes, mostrando visualmente que restam poucas vagas

